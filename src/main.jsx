import React from "react";
import ReactDOM from "react-dom/client";
import Proposal from "../Cornbread_Hemp_Proposal.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Proposal />
  </React.StrictMode>
);
